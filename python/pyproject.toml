
# ==== black ====
[tool.black]
line-length = 200
target-version = ['py36']

[tool.isort]
profile = "black"

[tool.pylint."MESSAGES CONTROL"]
disable = [
    "missing-docstring",
    "invalid-name",
]